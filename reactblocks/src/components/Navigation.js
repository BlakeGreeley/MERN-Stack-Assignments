

const navigationStyle = {
    background: 'blue',
    height: '400px',
    width: '200px',
    margin: '30px',
    border: 'none'
};

const Navigation = (props) => {
    return (
        <div style={navigationStyle}>
            
        </div>
    )
}

export default Navigation;