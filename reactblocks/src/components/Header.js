import React, { Component } from 'react';

const headerStyle = {
    background: 'green',
    height: '100px',
    width: '1000px',
    margin: '30px',
    border: 'none'
};

const Header = (props) => {
    return (
        <div style={headerStyle}>
            
        </div>
    )
}

export default Header;